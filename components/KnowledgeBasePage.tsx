import React from 'react';

const KnowledgeBasePage: React.FC = () => {
  return (
    <div>
      {/* This page is now empty as per user request. The link to the knowledge base will be in the main footer. */}
    </div>
  );
};

export default KnowledgeBasePage;
